<mod-ssl><!-- -*- nxml -*- -->
  <certificates>
    <!-- <cert serverName="localhost" 
	       crt="localhost.crt" 
	       key="localhost.key" 
	       [ chain="localhost.chain.crt" ]
	       [ ca="localhost.root.ca.crt" ]
	       [ verify-peer="yes" ] 
	       [ default="yes" ]
	       /> -->
    <!-- use certificates used by myqtt-regression-listener -->
    <cert serverName="localhost" crt="../test/localhost-server.crt" key="../test/localhost-server.key" />
  </certificates>
</mod-ssl>