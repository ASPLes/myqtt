<mod-ssl debug='no'><!-- -*- nxml -*- -->
  <!-- set debug='yes' to have more information about the module
       dropped into the log -->
  <certificates>
    <!-- <cert serverName="localhost" 
	       crt="localhost.crt" 
	       key="localhost.key" 
	       [ chain="localhost.chain.crt" ]
	       [ ca="localhost.root.ca.crt" ]
	       [ verify-peer="yes" ] 
	       [ default="yes" ]
	       /> -->
    <cert serverName="localhost" crt="localhost.crt" key="localhost.key" />
  </certificates>
</mod-ssl>
